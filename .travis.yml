language: julia
os:
  - linux
  - osx
julia:
  - 0.6
  - 0.7
  - 1.0
notifications:
  email: false
# uncomment the following lines to override the default test script
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia -e 'Pkg.clone(pwd()); Pkg.build("ConicBenchmarkUtilities"); Pkg.test("ConicBenchmarkUtilities"; coverage=true)'
after_success:
  - julia -e '(VERSION >= v"0.7" && using Pkg); Pkg.add("Coverage"); cd(Pkg.dir("ConicBenchmarkUtilities")); using Coverage; Codecov.submit(process_folder())'
